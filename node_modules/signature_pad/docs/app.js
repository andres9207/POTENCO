var wrapper = document.getElementById("signature-pad");
var clearButton = wrapper.querySelector("[data-action=clear]");
var changeColorButton = wrapper.querySelector("[data-action=change-color]");
var undoButton = wrapper.querySelector("[data-action=undo]");
var savePNGButton = wrapper.querySelector("[data-action=save-png]");
//var saveJPGButton = wrapper.querySelector("[data-action=save-jpg]");
var saveSVGButton = wrapper.querySelector("[data-action=save-svg]");
var body = $(".signature-pad--body");
var canvas = wrapper.querySelector("canvas");
/*var signaturePad = new SignaturePad(canvas, {
  // It's Necessary to use an opaque color when saving image as JPEG;
  // this option can be omitted if only saving as PNG or SVG
  //backgroundColor: 'rgb(255, 255, 255)'
});*/


// Adjust canvas coordinate space taking into account pixel ratio,
// to make it look crisp on mobile devices.
// This also causes canvas to be cleared.
/*function resizeCanvas() {
  console.log("Entro aca");
  // When zoomed out to less than 100%, for some very strange reason,
  // some browsers report devicePixelRatio as less than 1
  // and only part of the canvas is cleared then.
  var ratio =  Math.max(window.devicePixelRatio || 1, 1);

  // This part causes the canvas to be cleared
  canvas.width = canvas.offsetWidth * ratio;
  canvas.height = canvas.offsetHeight * ratio;
  canvas.getContext("2d").scale(ratio, ratio);

  // This library does not listen for canvas changes, so after the canvas is automatically
  // cleared by the browser, SignaturePad#isEmpty might still return false, even though the
  // canvas looks empty, because the internal data of this library wasn't cleared. To make sure
  // that the state of this library is consistent with visual state of the canvas, you
  // have to clear it manually.
  signaturePad.clear();
}

// On mobile devices it might make more sense to listen to orientation change,
// rather than window resize events.
window.onresize = resizeCanvas;
setTimeout(resizeCanvas(),2000);*/


var signaturePad = new SignaturePad(canvas);
/*signaturePad.minWidth = 5;
signaturePad.maxWidth = 10;*/


function resizeCanvas() {
  var ratio =  Math.max(window.devicePixelRatio || 1, 1);
  console.log("ratio" + ratio + "width" + canvas.offsetWidth); 
  if(canvas.offsetWidth<=0)
  {
    canvas.width = 664;// body.width();
    canvas.height = 358;// body.height();
  }
  else{
    canvas.width = canvas.offsetWidth * ratio;
    canvas.height = canvas.offsetHeight * ratio;
  }
  canvas.getContext("2d").scale(ratio, ratio);
  signaturePad.clear(); // otherwise isEmpty() might return incorrect value
}

window.addEventListener("resize", resizeCanvas);
resizeCanvas();

function download(dataURL, filename) {
  if (navigator.userAgent.indexOf("Safari") > -1 && navigator.userAgent.indexOf("Chrome") === -1) {
    window.open(dataURL);
  } else {
    var blob = dataURLToBlob(dataURL);
    var url = window.URL.createObjectURL(blob);

    var a = document.createElement("a");
    a.style = "display: none";
    a.href = url;
    a.download = filename;

    document.body.appendChild(a);
    a.click();

    window.URL.revokeObjectURL(url);
  }
}

// One could simply use Canvas#toBlob method instead, but it's just to show
// that it can be done using result of SignaturePad#toDataURL.
function dataURLToBlob(dataURL) {
  // Code taken from https://github.com/ebidel/filer.js
  var parts = dataURL.split(';base64,');
  var contentType = parts[0].split(":")[1];
  var raw = window.atob(parts[1]);
  var rawLength = raw.length;
  var uInt8Array = new Uint8Array(rawLength);

  for (var i = 0; i < rawLength; ++i) {
    uInt8Array[i] = raw.charCodeAt(i);
  }

  return new Blob([uInt8Array], { type: contentType });
}

clearButton.addEventListener("click", function (event) {
  signaturePad.clear();
});

undoButton.addEventListener("click", function (event) {
  var data = signaturePad.toData();

  if (data) {
    data.pop(); // remove the last dot or line
    signaturePad.fromData(data);
  }
});

changeColorButton.addEventListener("click", function (event) {
  var r = Math.round(Math.random() * 255);
  var g = Math.round(Math.random() * 255);
  var b = Math.round(Math.random() * 255);
  var color = "rgb(" + r + "," + g + "," + b +")";

  signaturePad.penColor = color;
});

function clearFirma()
{
  signaturePad.clear();
}

savePNGButton.addEventListener("click", function (event) 
{
  var tipo = $('#tipofirma').val();
  if (signaturePad.isEmpty()) {
    error("Por favor ingrese una firma primero.");  
    //alert("Please provide a signature first.");
  }
  else
  {
    var dataURL = signaturePad.toDataURL(); 
    
    $('#imgfirmaaprobador').attr('src',dataURL);
    //$('#imgfirmacontratista').focus();
    $('#firmaaprobador').val(dataURL);
    $('#firmaaprobador').parsley().validate();  
    $('#divfirmas').addClass('hide');
		$('#divimgfirma').removeClass('hide');
  }
});
function GUARDARCONTRATO(idcotizacion,tip)

//savePNGButton.on("click", function (event) 
{
  $('#frmcontrato').parsley().validate("grucontrato");
	if ($('#frmcontrato').parsley().isValid({group:'grucontrato'}))
	{

      var zona = $('input:radio[name="radzona"]:checked').val();
      var tipovia = $('input:radio[name="radtipovia"]:checked').val();
      var km = $('#txtkilometros').val();
      var tie = $('#txttiempo').val();
      var con = $('#txtcondiciones').val().replace(new RegExp("\n","g"), "<br>");
      var esp = $('#txtespecificaciones').val().replace(new RegExp("\n","g"),"<br>");
      var tpc = $('input:radio[name="radtipoconstruccion"]:checked').val();

      var trans = $('input:radio[name="radtransporte"]:checked').val();//cubre transporte
      var auttrans = $('input:radio[name="auttransporte"]:checked').val();//autorizo transporte
      var autcorreo = $('input:radio[name="radautorizocorreo"]:checked').val();//autorizo envio correo
      var ciu = $('#txtciudad2').val();
      var dia = $('#txtdias').val();
      var mes = ""; //$('#selmes').val();
      var ano = ""; //$('#txtano3').val();
      var doccontratista = $('#txtdoccontratista').val();
      var ciucontratista = $('#txtciucontratista').val();
      var doccontratante = $('#txtdoccontratante').val();
      var ciucontratante = $('#txtciucontratante').val();
      var doctestigo = $('#txtdoctestigo').val();
      var ciutestigo = $('#txtciutestigo').val();      
      var firmaautoriza = $('#firmaautoriza').val();
      var firmacontratista = $('#firmacontratista').val();
      var firmacontratante = $('#firmacontratante').val();
      var firmatestigo = $('#firmatestigo').val();
      var firmafinal = $('#firmafinal').val();

      var temporalidad = $('#txttemporalidad').val();

      var nom = $('#txtnombre').val();
      var doc = $('#txtdocumento').val();
      var tel = $('#txttelefono').val();
      var cor = $('#txtcorreo').val();
      var dom = $('#txtdomiciliocontrato').val();
      var dir = $('#txtdireccioncontrato').val();
      var bar = $('#txtbarrio').val();
      var mun = $('#txtmunicipio').val();
      var dep = $('#txtdepartamento').val();
      
  
  /*if (signaturePad.isEmpty()) {
    error("Por favor ingrese una firma primero.");  
    //alert("Please provide a signature first.");
  } */
       //var dataURL = signaturePad.toDataURL();    
    //download(dataURL, "signature.png");
      $.post('funciones/cotizacion/fnContratos.php',{
        opcion: "GUARDARCONTRATO",
        id:idcotizacion,
        //firma:dataURL,
        zona:zona,
        tipovia:tipovia,
        km:km,
        tie:tie,
        con:con,
        trans:trans,
        auttrans:auttrans,
        autcorreo:autcorreo,
        ciu:ciu,
        dia:dia,
        mes:mes,
        ano:ano,
        doccontratista:doccontratista,
        ciucontratista:ciucontratista,
        doccontratante:doccontratante,
        ciucontratante:ciucontratante,
        doctestigo:doctestigo,
        ciutestigo:ciutestigo,
        firmaautoriza:firmaautoriza,
        firmacontratista:firmacontratista,
        firmacontratante:firmacontratante,
        firmatestigo:firmatestigo,
        firmafinal:firmafinal,
        temporalidad:temporalidad,
        especificaciones:esp,
        tpc:tpc,
        tip:tip,
        nom:nom,
        doc:doc,
        tel:tel,
        cor:cor,
        dom:dom,
        dir:dir,
        bar:bar,
        mun:mun,
        dep:dep
      },function(data){
        var res = data[0].res;
        var msn  = data[0].msn;
        var env = data[0].env;
        if(res=="ok")
        {
          signaturePad.clear();
          if(tip==1 || tip==2)
          {
            Swal.fire({
              title: 'GRACIAS POR CONFIAR EN NOSOTROS.!!..Su sueño es nuestro proyecto',
              text: msn,
              icon: 'success',
              showCancelButton: false,
              confirmButtonColor: '#3085d6',
              cancelButtonColor: '#d33',
              confirmButtonText: 'Aceptar',
              allowOutsideClick: false,
            }).then((result) => {
              if (result.value) {
                window.close();
              }
            })
            if(env==1)
            {
              setTimeout(CRUDCONTRATOS('ENVIARAPROBACION',idcotizacion,tip),500)
            }
          }
          else
          {
            $('#mymodals').modal('hide');
            CRUDCOTIZACION('CONTESTACOTI');
            if(env==1)
            {
              setTimeout(CRUDCONTRATOS('ENVIARAPROBACION',idcotizacion,tip),500)
            }
            ok(msn);
          }
        }
        else
        {
          error(msn);
        }
        
      },"json")

  }
  else{
    error("Tiene información obligatoria pendiente por diligenciar. Verificar");
  }
}//);
/*
saveJPGButton.addEventListener("click", function (event) {
  if (signaturePad.isEmpty()) {
    alert("Please provide a signature first.");
  } else {
    var dataURL = signaturePad.toDataURL("image/jpeg");
    download(dataURL, "signature.jpg");
  }
});

saveSVGButton.addEventListener("click", function (event) {
  if (signaturePad.isEmpty()) {
    alert("Please provide a signature first.");
  } else {
    var dataURL = signaturePad.toDataURL('image/svg+xml');
    download(dataURL, "signature.svg");
  }
});*/
